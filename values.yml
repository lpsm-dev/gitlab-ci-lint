# =============================================================================
# VALUES DEFINITION
# =============================================================================

---
gitlab:
  endpoint: "https://gitlab.com/api/v4/ci/lint"
  timeout: 10
  file: ".gitlab-ci.yml"
log:
  file: "file.log"
